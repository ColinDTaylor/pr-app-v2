'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var _api = new WeakMap();

var Matches = function () {
  function Matches(parent) {
    _classCallCheck(this, Matches);

    _api = parent;
  }

  _createClass(Matches, [{
    key: 'index',
    value: function index(tid) {
      var params = {};

      return new Promise(function (resolve, reject) {
        _api.request('GET', 'tournaments/' + tid + '/matches.json', params).then(function (response) {
          //remove 1st layer "matches"
          resolve(response.map(function (obj) {
            return obj.match;
          }));
        }).catch(function (err) {
          reject(err.message);
        });
      });
    }
  }, {
    key: 'show',
    value: function show(tid, mid) {
      var params = {};

      return new Promise(function (resolve, reject) {
        _api.request('GET', 'tournaments/' + tid + '/matches/' + mid + '.json', params).then(function (response) {
          resolve(response.match);
        }).catch(function (err) {
          reject(err.message);
        });
      });
    }

    //http://api.challonge.com/v1/documents/matches/update

  }, {
    key: 'update',
    value: function update(tid, mid, params) {
      return new Promise(function (resolve, reject) {
        _api.request('PUT', 'tournaments/' + tid + '/matches/' + mid + '.json', params).then(function (response) {
          resolve(response.match);
        }).catch(function (err) {
          reject(err.message);
        });
      });
    }
  }]);

  return Matches;
}();

exports.default = Matches;